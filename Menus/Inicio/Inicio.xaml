<Page x:Class="V1P3R.Menus.Inicio.Inicio"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:V1P3R.Menus.Inicio"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800"
        Title="Inicio" Loaded="Page_Loaded" Background="#FF4565">
    <Grid>
        <MediaElement x:Name="videoBackground" Height="450" Volume="0" Loaded="videoBackground_Loaded" MediaEnded="videoBackground_MediaEnded" LoadedBehavior="Manual" VerticalAlignment="Top" OpacityMask="#FFDA1414" Stretch="UniformToFill" >
            <MediaElement.Triggers>
                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                    <BeginStoryboard>
                        <Storyboard>

                            <MediaTimeline x:Name="videoContainer" Source="" Storyboard.TargetName="videoBackground"  
                            RepeatBehavior="Forever" />

                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </MediaElement.Triggers>
        </MediaElement>
    </Grid>
</Page>
